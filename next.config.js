/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  reactStrictMode: false,
  swcMinify: true,

  /*
  output: "export",
  basePath: "/out",
  */
};

module.exports = nextConfig;
